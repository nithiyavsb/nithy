#include<stdio.h>
int main()
{
int positive,negative;
scanf(:%d%d;&positive,&negative);
printf("enter the two (intervals):");
printf("prime number b/w %d and %d:",positive,negative);
while(positive<negative)
for(i=2;i<=negative/2 ++i);
if(negative%i==0)
break;
printf("%d",negative);
++negative;
return o;
}
